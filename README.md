### install
    pip install pygame
    pip install opencv-python
    pip install tensorflow
    pip install matplotlib

### data folder
    < data >
        < model name >
            < img_full >
            < img_frame >
            < models >
            < model.json>